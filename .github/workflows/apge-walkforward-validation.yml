name: APGE Walk-forward Validation

on:
  push:
    branches:
      - apge-prelive-completion-v1
    paths:
      - 'scripts/apge_walkforward_validation.py'
      - 'src/apge/backtest.py'
      - 'src/apge/binance_public_data.py'
      - 'src/apge/market_regime.py'
      - 'src/apge/adaptive_policy.py'
      - 'src/apge/grid_strategy.py'
      - '.github/workflows/apge-walkforward-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  walkforward:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install runtime dependency
        run: python -m pip install --upgrade pip requests

      - name: Run read-only multi-window validation
        env:
          PYTHONPATH: src
        run: python scripts/apge_walkforward_validation.py

      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: apge-walkforward-validation
          path: apge_walkforward_validation.json
          if-no-files-found: error
